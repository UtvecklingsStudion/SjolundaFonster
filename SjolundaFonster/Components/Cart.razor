@inject SjolundaFonster.Services.CartService CartService;

<CartIcon Click="@(() => Open = true)"></CartIcon>
<CartOverview @bind-Open="@Open"></CartOverview>
    

@code {
    public bool Open { get; set; }
}